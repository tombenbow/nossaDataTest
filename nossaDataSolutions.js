// Find the Duplicate 
// I assumed here there always would be a duplicate

const findTheDuplicate = () => {
    return 0
}

//Longest Consecutive 1s
// I assumed here the input would always be a positive integer

const longestConsecutive1s = (x) => {
    return parseInt(x.toString(2)) //in JS, this converts a number to it's binary representation
}

module.exports = {
    findTheDuplicate,
    longestConsecutive1s
}